/**
 * Report-problem UI: option lists (same tuples as `@/constants/complaints`).
 */

export {
    COMPLAINT_CATEGORY_VALUES as COMPLAINT_CATEGORIES,
    COMPLAINT_PRIORITY_VALUES as COMPLAINT_PRIORITIES,
} from '@/constants/complaints';
